{
    "type": "object",
    "title": "Device",
    "description": "A device in the game",
    "properties": {
        "name": {
            "type": "string",
            "description": "Name of the device"
        },
        "image": {
            "type": "string",
            "description": "Path to the image asset corresponding to the device"
        },
        "interactions": {
            "type": "array",
            "items": {
                "$ref": "numericalActionScheme.json"
            }
        },
        "position": {
            "$ref": "devicePositionSchema.json"
        },
        "visualState": {
            "type": "array",
            "items": {
                "$ref": "visualStateSchema.json"
            },
            "minItems": 1
        }
    },
    "required": ["name", "image", "interactions", "position", "visualState"],
    "additionalProperties": false
}